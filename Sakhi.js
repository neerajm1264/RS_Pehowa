const toggleButton=document.getElementById("toggleButton"),navLinks=document.getElementById("navLinks"),headline=document.getElementById("headline"),links=document.querySelectorAll(".nav-links li");let previousMargin="2rem 0";toggleButton.addEventListener("click",()=>{navLinks.classList.toggle("active")}),links.forEach(e=>{e.addEventListener("click",()=>{navLinks.classList.remove("active")})});let http=new XMLHttpRequest;http.open("get","sakhi.json",!0),http.send(),http.onload=function(){if(4==this.readyState&&200==this.status){let e=JSON.parse(this.responseText),t="";e.forEach(e=>{t+=`
            <p class="a">${e.data}</p><hr> 
            `}),document.querySelector(".quote").innerHTML=t}};